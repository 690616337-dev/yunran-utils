name: Build and Release

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-macos-arm64:
    runs-on: macos-latest
    timeout-minutes: 60
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        npm install
        cd frontend && npm install && cd ..
        pip install -r backend/requirements.txt pyinstaller

    - name: Build frontend
      run: npm run build:frontend

    - name: Build Backend (PyInstaller ARM64)
      run: |
        cd backend
        # 构建ARM64版本（在ARM64运行器上原生构建）
        pyinstaller --onefile --name backend --target-arch arm64 main.py
        # 验证构建的二进制文件架构
        echo "验证后端二进制文件架构:"
        file dist/backend
        mkdir -p ../resources/backend
        cp dist/backend ../resources/backend/
        cd ..

    - name: Build Electron (ARM64)
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        npx electron-builder --mac dmg --arm64
        # 验证构建的APP架构
        echo "验证Electron APP架构:"
        file dist/*.dmg || true
        ls -la dist/

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: macOS-ARM64
        path: dist/*.dmg

  build-windows:
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, arm64]
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      shell: bash
      run: |
        npm install
        cd frontend && npm install && cd ..
        pip install -r backend/requirements.txt pyinstaller

    - name: Build frontend
      run: npm run build:frontend

    - name: Build Backend (PyInstaller)
      shell: bash
      run: |
        cd backend
        if [ "${{ matrix.arch }}" == "arm64" ]; then
          pyinstaller --onefile --name backend --target-arch arm64 main.py
        else
          pyinstaller --onefile --name backend main.py
        fi
        mkdir -p ../resources/backend
        cp dist/backend.exe ../resources/backend/
        cd ..

    - name: Build Electron
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      shell: bash
      run: |
        if [ "${{ matrix.arch }}" == "x64" ]; then
          npx electron-builder --win nsis --x64
        else
          npx electron-builder --win nsis --arm64
        fi

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: Windows-${{ matrix.arch }}
        path: dist/*.exe
